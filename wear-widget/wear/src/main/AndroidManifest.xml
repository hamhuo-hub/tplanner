<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:label="tPlanner"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="false">

        <!-- Watch Face -->
        <service
            android:name=".TplannerWatchFace"
            android:label="tPlanner"
            android:permission="android.permission.BIND_WATCH_FACE_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watch_face_preview" />
        </service>

        <!-- Tile -->
        <service
            android:name=".EventTileService"
            android:label="今日便签"
            android:exported="true"
            android:permission="com.google.android.wearable.permission.BIND_TILE_PROVIDER">
            <intent-filter>
                <action android:name="androidx.wear.tiles.action.BIND_TILE_PROVIDER" />
            </intent-filter>
            <meta-data
                android:name="androidx.wear.tiles.PREVIEW"
                android:resource="@drawable/tile_preview" />
        </service>

        <!-- Data Layer listener -->
        <service
            android:name=".DataLayerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <data android:scheme="wear" android:host="*" android:pathPrefix="/tplanner" />
            </intent-filter>
        </service>

    </application>
</manifest>
